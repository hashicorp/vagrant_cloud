language: ruby
sudo: false
dist: xenial

rvm:
  - 2.3
  - 2.4
  - 2.5
  - 2.6
  - ruby-head

matrix:
  allow_failures:
    - rvm: ruby-head

install:
  - bundle install

script:
  - bundle exec rake

notifications:
  email: false
